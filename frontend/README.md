# Eigenflow Web

React, TypeScript, Vite, and D3 presentation layer for Eigenflow.

```powershell
npm install
npm run dev
```

The development server proxies `/api` to the Python service at `http://localhost:8000`.
